# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting
'on':
  push:
    branches:
      - develop
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Make env file
        uses: SpicyPizza/create-envfile@v1.3
        with:
          envkey_VITE_FIRESTORE_API_KEY: ${{ vars.FIRESTORE_API_KEY }}
          envkey_VITE_FIRESTORE_APP_ID: ${{ vars.FIRESTORE_APP_ID }}
          envkey_VITE_FIRESTORE_AUTH_DOMAIN: ${{ vars.FIRESTORE_AUTH_DOMAIN }}
          envkey_VITE_FIRESTORE_MESSAGING_SENDER_ID: ${{ vars.FIRESTORE_MESSAGING_SENDER_ID }}
          envkey_VITE_FIRESTORE_PROJECT_ID: ${{ vars.FIRESTORE_PROJECT_ID }}
          envkey_VITE_FIRESTORE_STORAGE_BUCKET: ${{ vars.FIRESTORE_STORAGE_BUCKET }}        
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Build the project
        run: yarn build
      - name: Archive Production Artifact
        uses: actions/upload-artifact@v3
        with:
          name: dist
          path: dist
  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Download Artifact
        uses: actions/download-artifact@v3
        with:
          name: dist
          path: dist
      - name: Deploy to firestore
        uses: w9jds/firebase-action@master
        with:
          args: deploy
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SANYCH_MUSTACHE_DEV }}
